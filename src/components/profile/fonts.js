// The pilot-profile pages use every brand face. Loaded once in src/lib/brandFonts.js
// so the footer (which renders on every public page) shares the same requests.
export { brandFontClass as profileFontClass } from '@/lib/brandFonts'
